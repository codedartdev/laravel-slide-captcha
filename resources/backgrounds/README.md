# Slide CAPTCHA Backgrounds

This directory contains the package's default local background images.

Set `SLIDE_CAPTCHA_BACKGROUNDS_PATH` to an absolute path, or to a path relative to the Laravel app base path, to replace these defaults with your own directory.
